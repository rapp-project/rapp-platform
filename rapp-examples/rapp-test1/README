main.js is the Original Entry Point (OEP) for the RAPP `test1`.
As a HOP package, the OEP is always going to be `main.js` which will be automatically generated by the RAPP Submission Service (RAPP-Store).

rapp.hpp is a mock-up of the RAPP::API for C++
rapp.js is a mock-up of the RAPP::API for Javascript

test1.cpp is a mock-up of a RAPP written in C++ (compile with: g++ -std=c++11 -o test1)
test1.js is a mock-up of a RAPP written in Javascript

For questions or comments, send me an email.
