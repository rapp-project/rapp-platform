Instuctions on how to execute the test:
1) Run rosbridge_websocket_server:
  ``` roslaunch rosbridge_server rosbridge_websocket.launch ```

2) Run the faceDetection ROS-Node (from rapp-platform)
  ``` roslaunch rapp_face_detection face_detection.launch ```

3) Run the test with hop:
  ``` hop -v -g -p 9001 test.js ```

*** You might need to change the following variables located in the test.js file ***
- faceImage --line3
